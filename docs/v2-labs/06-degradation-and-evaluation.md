# Lab 6：降级与评测

## 目标

区分可降级能力和必须失败关闭的关键证据，并理解 QDSR、确认转化、草案成功率与六层 guardrail。

## 观察

运行广告故障、取消、预算不足和证据缺失测试，比较最终状态与 Trace。

## 动手

增加一个 Recommendation 超时用例：单品搜索仍可继续，要求完整套装时必须因品类覆盖不足被拒绝。不要用放宽硬约束作为兜底。随后检查合成检索集中的 `relevance_grades`，说明 NDCG 的理想排序为什么必须来自完整 qrels，而不是只对系统已返回的商品重新排序。

## 检查

```bash
bash scripts/check-v2-lab.sh 6
```

完成证据：列出一个“继续但标记降级”的例子和一个“必须拒绝”的例子，并说明原因；同时解释合成规则标签与人工金标的区别，以及为什么两者都需要。
