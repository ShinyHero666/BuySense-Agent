# Lab 2：意图与约束

## 目标

区分显式用户约束、模型推断和系统策略，理解模型提案为什么必须经过校正。

## 观察

提交“预算 5000 元，不要广告，选拍照手机”，查看约束的 `source / strength / confidence / status`。

## 动手

在 `model-policy.test.ts` 增加失败样本：模型试图删除“不展示广告”或缩小用户明确要求的品类。测试必须证明原始 Query 和显式约束仍然保留。

## 检查

```bash
bash scripts/check-v2-lab.sh 2
```

完成证据：说明“模型建议”和“最终检索计划”为什么不是同一个对象。
