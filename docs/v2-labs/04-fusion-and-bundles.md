# Lab 4：融合与组合

## 目标

理解多路候选为什么不能直接拼接，以及逐类贪心为什么可能输给全局组合优化。

## 观察

阅读 Python 的 `fuse()` 与 `optimize_bundles()`，比较 Python 主实现和 TypeScript 降级实现的契约。

## 动手

新增一个候选集：最高出价广告相关性不足，另一个自然结果分数更高；验证 Top 3 广告上限、显式标注和自然结果保护。再构造一个逐类最便宜但整体不兼容的组合。

## 检查

```bash
bash scripts/check-v2-lab.sh 4
```

完成证据：用自己的数字手算一次 RRF，并说明最终组合为何不是逐项最高分。
