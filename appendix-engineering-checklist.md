# 附录：搜索与推荐工程验收清单

这份清单用于方案评审、上线前检查和事故复盘。它不是阶段说明的替代品；不理解某一项时，返回对应章节和代码实验。

## 1. 请求与契约

- [ ] 请求字段有明确类型、范围、默认值和错误结构。
- [ ] Query、用户、场景、分页和排序模式经过校验。
- [ ] 请求 ID、实验版本和配置版本可追踪。
- [ ] 候选对象能够保留召回来源、特征、分数和重排理由。
- [ ] 响应契约区分业务空结果和系统异常。

## 2. Query 理解

- [ ] 归一化不会破坏品牌、型号、数字和单位。
- [ ] 同义词和类目改写有版本、灰度和回滚机制。
- [ ] 泛 Query 使用证据判定，而不是只看字符串长度。
- [ ] 风险词、敏感词和非法请求有明确处理顺序。
- [ ] Query 日志遵守隐私、脱敏和保留期限要求。

## 3. 召回

- [ ] 每路召回的目标、索引、超时和候选上限清楚。
- [ ] 多路结果按稳定业务键去重，同时保留全部来源。
- [ ] 召回器超时可以保留已完成结果并触发兜底。
- [ ] 热门兜底不会绕过库存、安全和权限过滤。
- [ ] 能按召回源查看覆盖率、重叠率、有效率和延迟。

## 4. 过滤

- [ ] 硬过滤与软策略分开，执行顺序可解释。
- [ ] 每个过滤器输出结构化理由码和数量。
- [ ] 召回后过滤和返回前最终状态校验职责清楚。
- [ ] 过滤器异常不会静默放行安全风险。
- [ ] 数量守恒：输入数等于保留数加各理由过滤数。

## 5. 特征与排序

- [ ] 特征有名称、类型、口径、时效、默认值和负责人。
- [ ] 训练与在线推理使用一致的特征定义和版本。
- [ ] 粗排成本显著低于精排，并有明确截断规模。
- [ ] pCTR、pCVR、价值和质量信号经过校准后再组合。
- [ ] 模型超时或异常时能退化到上一排序层。
- [ ] 排序输出包含模型版本、特征版本和分数解释。

## 6. 重排与流量池

- [ ] 模型重排和规则重排职责分离。
- [ ] 安全、库存和权限约束不可被模型绕过。
- [ ] 类目、卖家、品牌和相似商品频控有明确窗口。
- [ ] Flow Pool 配额能转成总和严格等于页面大小的整数目标。
- [ ] 候选不足时有跨池补量策略，并同时记录目标值和实际值。
- [ ] 能解释每个商品重排前后位置及原因。

## 7. 推荐状态与反馈

- [ ] 曝光去重按用户、场景和商品隔离。
- [ ] 曝光记录有 TTL，避免候选永久枯竭。
- [ ] 点击、收藏、负反馈、购买的语义和权重不同。
- [ ] 事件写入具备幂等键、时间戳和顺序处理策略。
- [ ] 用户快照更新线程安全，读取不会泄漏可变内部状态。
- [ ] 反馈效果可通过固定场景回放验证。

## 8. 配置与实验

- [ ] 配置在启动或发布前进行结构和语义校验。
- [ ] Pipeline 只有一个权威执行顺序。
- [ ] 用户分桶稳定，实验层之间按设计互斥或正交。
- [ ] 流量之和、权重之和和参数范围有自动检查。
- [ ] 实验曝光记录发生在用户真正进入策略时。
- [ ] 发布具备小流量灰度、监控门槛、自动停止和快速回滚。

## 9. 可观测性

- [ ] 每阶段记录耗时、输入数、输出数、版本和降级状态。
- [ ] 指标同时覆盖流量、延迟、错误、候选数量和业务效果。
- [ ] Trace 能串联入口、召回、特征、模型和事件反馈。
- [ ] 日志可采样且不泄漏敏感数据。
- [ ] 告警包含定位线索、影响范围和处理手册。
- [ ] 有空结果、候选突降、模型超时和配置错误的演练。

## 10. API 与运行环境

- [ ] 服务监听地址和端口可配置。
- [ ] 存活、就绪和启动探针职责分离。
- [ ] 请求体大小、并发数、超时和重试有上限。
- [ ] 容器以非 root 用户运行，镜像内容最小化。
- [ ] CPU/内存 requests、limits 和 HPA 指标经过压测校准。
- [ ] 发布过程支持滚动升级、PodDisruptionBudget 和快速回滚。

## 11. 数据与模型闭环

- [ ] 商品变更可增量进入索引，并处理删除与乱序事件。
- [ ] 行为事件具备 schema、质量监控和迟到处理。
- [ ] 训练样本避免标签泄漏和曝光选择偏差。
- [ ] 离线评估、历史回放、灰度和在线实验形成发布门禁。
- [ ] 模型、特征、代码和配置版本能够联合复现。
- [ ] 有数据漂移、特征缺失和模型效果衰减监控。

## 12. 二手业务专项

二手墨圆质选、回收和寄卖场景还需要检查：

- [ ] 未通过质检的商品不能进入搜索、推荐或下单。
- [ ] 质检报告版本与用户下单时看到的内容可追溯。
- [ ] 电池、维修、非原装件和功能故障使用结构化字段。
- [ ] 预估价明确标记为质检前区间，不伪装成最终成交价。
- [ ] 预估价到最终价的变化有因素解释和申诉/退回路径。
- [ ] 寄卖商品的取件、质检、仓储、上架、售后和结算状态一致。
- [ ] 线上索引、门店、仓库和下单权威库存不会导致重复售卖。
- [ ] 信任特征不能替代安全、合规和质检硬过滤。

## 13. 上线结论模板

一次可批准的上线评审至少应回答：

1. 改动影响哪个阶段、场景和用户范围？
2. 候选数量、延迟、相关性和业务指标预计如何变化？
3. 使用什么离线测试、场景回放和小流量实验验证？
4. 哪些依赖可能失败，分别如何降级？
5. 观察哪些指标，告警阈值和停止条件是什么？
6. 如何在分钟级回滚代码、模型和配置？

只有“功能能够运行”还不够；可解释、可观测、可灰度、可回滚才是上线条件。
# 搜推 Agent 增补检查

- [ ] Agent 名称和范围明确是购买决策搜推，不是万能品牌助手。
- [ ] 硬约束由结构化状态维护，模型不能静默修改。
- [ ] 写工具需要显式确认、幂等键和用户/会话隔离。
- [ ] 商品事实和规则分别引用工具证据与版本化知识卡。
- [ ] 商品内容按不可信输入处理，注入样本有回归测试。
- [ ] Trace 包含工作流、Prompt、工具合同和 ModelPort 路由版本。
- [ ] Replay 模式在无网络 CI 中确定性通过。
- [ ] 正常/失败/安全评测满足第 16 章门禁。
- [ ] ModelPort 失败只触发一次应用层确定性降级，不形成重试风暴。
- [ ] 诊断 Agent 只有只读权限，不能修改实验、发布、订单或用户数据。
