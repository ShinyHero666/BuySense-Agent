spring:
  datasource:
    url: jdbc:h2:mem:buysense-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  flyway:
    enabled: true
  jackson:
    default-property-inclusion: non_null
    deserialization:
      fail-on-unknown-properties: true

buysense:
  session-secret: test-session-secret-at-least-16
  domain:
    default-bundle:
      - phone
      - headphones
      - charger
